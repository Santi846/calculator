import React from "react";
import '../components/styles/terminal.css';

export const Terminal = ({input}) => {

    return(
        <div className='input'>
            {input}
        </div>
    );
};